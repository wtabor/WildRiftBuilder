{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run typecheck && npm run validate-data && npm test && npm run build",
  "crons": [
    {
      "path": "/api/cron/patch-scan",
      "schedule": "0 12 * * *"
    }
  ]
}
